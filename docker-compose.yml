services:
  mysql:
    image: postgres:latest
    container_name: postgres_api_stock
    environment:
      POSTGRES_DB: apistock
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: